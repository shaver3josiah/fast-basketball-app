source "https://rubygems.org"

# Used only by .github/workflows/ios-testflight.yml to run `fastlane pilot`.
# No Gemfile.lock is committed, so bundler resolves fresh every run; pin the major
# so a future fastlane 3.0 cannot break a release build that changed nothing.
gem "fastlane", "~> 2.0"
